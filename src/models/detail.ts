// Models folder is created since detail.ts and post.ts will be used with the properties throughout the application

export class Detail {
  constructor(public name: string, public amount: number) {}
}
